global:
  scrape_interval: 15s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: ingestion-api
    static_configs:
      - targets: ["host.docker.internal:8080"]

  - job_name: worker
    static_configs:
      - targets: ["host.docker.internal:8081"]
